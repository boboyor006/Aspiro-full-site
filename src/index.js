import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';  // yoki boshqa ilova nomi

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
